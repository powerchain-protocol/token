# @powerchain/token-metadata

Canonical PWRC metadata builders and validation helpers. Generated metadata remains synchronized with `config/metadata.source.json`, public assets, Metaplex-compatible JSON, and Token-2022 metadata.
