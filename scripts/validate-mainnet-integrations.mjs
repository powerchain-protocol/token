import fs from "node:fs"; const c=JSON.parse(fs.readFileSync("config/mainnet-integrations.json","utf8")); if(c.cluster!=="mainnet-beta")throw new Error("mainnet integration cluster mismatch"); if(c.providers.rpc.primary!=="helius"||c.providers.explorer.default!=="solscan")throw new Error("provider defaults mismatch"); if(c.providers.marketData.authoritative||c.providers.oracle.authoritative||c.safety.externalDataMayChangeSupply)throw new Error("observational providers cannot be authoritative"); console.log("mainnet integrations validated");
