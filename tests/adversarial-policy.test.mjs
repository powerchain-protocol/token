import test from "node:test"; import assert from "node:assert/strict"; import { readFile } from "node:fs/promises";
const root=new URL("../",import.meta.url); const auth=JSON.parse(await readFile(new URL("config/authorities.json",root),"utf8")); const policy=JSON.parse(await readFile(new URL("config/release-policy.json",root),"utf8"));
test("single-signer and hot-wallet governance are forbidden",()=>{assert.equal(auth.controls.hotWalletAllowed,false);assert.equal(auth.separationOfDuties.singleSignerForbidden,true);assert.equal(auth.controls.threshold,"5-of-7")});
test("production is blocked without independent evidence",()=>{assert.equal(policy.productionActivation.default,false);assert.ok(policy.blockingArtifacts.some(x=>x.includes("audits/program")));assert.ok(policy.blockingArtifacts.some(x=>x.includes("economic-policy")))});
